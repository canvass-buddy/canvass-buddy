{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local", "$API_SECRET"],
  "pipeline": {
    "build": {
      "outputs": ["dist/**", ".next/**"],
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "clean": {
      "cache": false
    }
  }
}
